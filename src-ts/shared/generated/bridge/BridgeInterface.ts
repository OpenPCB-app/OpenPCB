// This file is auto-generated by scripts/gen-bridge.ts
// Do not edit manually
// @ts-nocheck

import { commands } from '@shared/generated/tauri-bindings';
import type * as Types from '@shared/generated/tauri-bindings';
import { listen } from '@tauri-apps/api/event';

export interface InvokeOptions {
  signal?: AbortSignal;
}

async function bridgeInvoke<T>(
  namespace: string,
  command: string,
  payload: unknown,
  opts?: InvokeOptions,
): Promise<T> {
  if (opts?.signal?.aborted) {
    throw new DOMException('The operation was aborted', 'AbortError');
  }

  const response = await commands.bridgeInvoke({ namespace, command, payload });

  if (response.status !== 'ok') {
    throw new Error(typeof response.error === 'string' ? response.error : 'Bridge invocation failed');
  }

  const bridgeResponse = response.data;
  if (bridgeResponse.status !== 'ok') {
    throw new Error(bridgeResponse.error?.message ?? 'Bridge command failed');
  }

  return bridgeResponse as T;
}

async function on<T>(namespace: string, eventName: string, cb: (p: T) => void): Promise<() => void> {
  return await listen<T>(`${namespace}:${eventName}`, (event) => cb(event.payload));
}

function toAsyncIterable<T>(
  namespace: string,
  eventName: string,
  opts?: { signal?: AbortSignal; correlationId?: string },
): AsyncIterable<T> {
  const queue: T[] = [];
  const waiters: Array<(result: IteratorResult<T>) => void> = [];
  let stopped = false;

  const emitValue = (value: T): void => {
    const waiter = waiters.shift();
    if (waiter) {
      waiter({ done: false, value });
      return;
    }
    queue.push(value);
  };

  const tauriEventName = `${namespace}:${eventName}`;
  const unlistenPromise = listen<unknown>(tauriEventName, (event) => {
    if (stopped) return;

    const payload = event.payload as unknown;
    if (opts?.correlationId) {
      const correlation = (payload as { correlationId?: unknown } | null)?.correlationId;
      if (correlation !== opts.correlationId) return;
    }

    const maybeChunk = (payload as { chunk?: T } | null)?.chunk;
    emitValue((maybeChunk ?? payload) as T);
  });

  const stop = async (): Promise<void> => {
    if (stopped) return;
    stopped = true;
    const unlisten = await unlistenPromise.catch(() => null);
    if (unlisten) unlisten();
    while (waiters.length > 0) {
      const waiter = waiters.shift();
      if (waiter) waiter({ done: true, value: undefined as never });
    }
  };

  if (opts?.signal) {
    if (opts.signal.aborted) {
      void stop();
    } else {
      opts.signal.addEventListener('abort', () => {
        void stop();
      }, { once: true });
    }
  }

  return {
    [Symbol.asyncIterator]() {
      return this;
    },
    async next(): Promise<IteratorResult<T>> {
      if (queue.length > 0) {
        return { done: false, value: queue.shift() as T };
      }
      if (stopped) {
        return { done: true, value: undefined as never };
      }
      return await new Promise<IteratorResult<T>>((resolve) => waiters.push(resolve));
    },
    async return(): Promise<IteratorResult<T>> {
      await stop();
      return { done: true, value: undefined as never };
    },
    async throw(error?: unknown): Promise<IteratorResult<T>> {
      await stop();
      throw error;
    },
  };
}


export interface coreNS {
  serverStatus(args?: null, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
}
export interface secretsNS {
  listProviderApiKeys(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  hasProviderApiKey(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  setProviderApiKey(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  removeProviderApiKey(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  syncProviderApiKey(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  syncProviderApiKeyRemoval(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  setAccountSession(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  getAccountSession(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  removeAccountSession(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  setEntitlementCache(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  getEntitlementCache(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  removeEntitlementCache(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  evaluateEntitlementState(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
}
export interface bunNS {
  status(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  getBackendUrl(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
  restart(args: any, opts?: InvokeOptions): Promise<Types.BridgeResponse>;
}

export const core: coreNS = {
  serverStatus: async (_args?: null, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('core', 'serverStatus', null, opts),
};
export const secrets: secretsNS = {
  listProviderApiKeys: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'listProviderApiKeys', args, opts),
  hasProviderApiKey: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'hasProviderApiKey', args, opts),
  setProviderApiKey: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'setProviderApiKey', args, opts),
  removeProviderApiKey: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'removeProviderApiKey', args, opts),
  syncProviderApiKey: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'syncProviderApiKey', args, opts),
  syncProviderApiKeyRemoval: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'syncProviderApiKeyRemoval', args, opts),
  setAccountSession: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'setAccountSession', args, opts),
  getAccountSession: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'getAccountSession', args, opts),
  removeAccountSession: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'removeAccountSession', args, opts),
  setEntitlementCache: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'setEntitlementCache', args, opts),
  getEntitlementCache: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'getEntitlementCache', args, opts),
  removeEntitlementCache: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'removeEntitlementCache', args, opts),
  evaluateEntitlementState: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('secrets', 'evaluateEntitlementState', args, opts),
};
export const bun: bunNS = {
  status: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('bun', 'status', args, opts),
  getBackendUrl: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('bun', 'getBackendUrl', args, opts),
  restart: async (args: any, opts?: InvokeOptions) => await bridgeInvoke<Types.BridgeResponse>('bun', 'restart', args, opts),
};

export interface BridgeInterface {
  core: coreNS;
  secrets: secretsNS;
  bun: bunNS;
}

export const bridge: BridgeInterface = {
  core: core,
  secrets: secrets,
  bun: bun,
};
