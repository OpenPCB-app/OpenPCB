// Master switch for OpenPCB Cloud accounts in the desktop app.
// The desktop app is fully usable offline today; flip to true when the
// cloud auth backend lands and the // TODO(cloud-auth) seams are wired.
export const CLOUD_AUTH_ENABLED = false;

// Inline copy surfaced when a user interacts with the (currently inert) form.
export const COMING_SOON_NOTICE =
  "Cloud accounts are coming soon. The desktop app is fully usable offline today.";
