/**
 * Re-export shim: the Radix dialog wrapper moved into the kit
 * (`@shared/frontend/ui/dialog`). Import from there in new code.
 */
export {
  Dialog,
  DialogBody,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
  type DialogContentProps,
  type DialogHeaderProps,
  type DialogSize,
} from "@shared/frontend/ui/dialog";
